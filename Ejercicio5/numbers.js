let altura = 168;
let alturaMt = altura / 100;
let peso = 5.98;
let alturaRedondeada = Math.round(alturaMt);
let pesoRedondeado = Math.floor(peso);

let min_valor_JS = Number.MIN_VALUE + 1;
let max_valor_JS = Number.MAX_VALUE;
let res = min_valor_JS === max_valor_JS ? true : false;
